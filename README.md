# Gatekeeper

Lightweight reverse proxy + session authentication gateway.

Quickstart

1. Build:

```bash
go build ./cmd/gatekeeper
```

2. Run with example config:

```bash
./gatekeeper -config config.example.yml
```
